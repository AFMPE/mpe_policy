name: Apply MPE Policy plan

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write

jobs:
  plan:
<<<<<<< HEAD
    runs-on: ubuntu-latest
    name: Apply terraform plan for an MPE Policy terraform configuration
=======
    if: ${{ github.event.label.name == 'management' }}
    runs-on: ubuntu-latest
    name: Apply terraform plan for an MPE Management terraform configuration
>>>>>>> 72f9a6ca5fd7d9d50a2da8903c6111fe2c19cc34
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      ARM_CLIENT_ID: ${{ secrets.AZURE_AD_CLIENT_ID }}
      ARM_CLIENT_SECRET: ${{ secrets.AZURE_AD_CLIENT_SECRET }}
      ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      ARM_TENANT_ID: ${{ secrets.AZURE_AD_TENANT_ID }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: terraform apply
        uses: azurenoops/terraform-github-actions/terraform-apply@v1.34.2
        with:
          path: src
<<<<<<< HEAD
          var_file: |
            src/tfvars/production/parameters.prod.tfvars
          backend_config: |
            storage_account_name=${{ secrets.AZURE_STORAGE_ACCOUNT_NAME }}
            container_name=${{ secrets.AZURE_STORAGE_CONTAINER_NAME }}            
            resource_group_name=${{ secrets.TFSTATE_RG }}
=======
>>>>>>> 72f9a6ca5fd7d9d50a2da8903c6111fe2c19cc34
